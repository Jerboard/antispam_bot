FROM python:3.13.5

# Установка системных зависимостей, включая tesseract и языковые паки
RUN apt-get update

WORKDIR /app

COPY /requirements/bot_stats.txt .
RUN pip install --no-cache-dir -r bot_stats.txt

COPY /bot_stats .

CMD ["sh", "-c", "python main.py"]